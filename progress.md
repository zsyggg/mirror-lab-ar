Original prompt: 用户希望把《镜面实验室》从黑暗第一视角搜证，重做成剧情类+推理类 minigame：参考 ringhyacinth/hyacinth.im-site 的 3D 物件/场景表现，改成可爱探测小机器人进入辐射房间搜查；减少人物不一致，增加剧情反转、不同结果、狼人杀式嫌疑人自辩和密室推理。

2026-05-26
- Observed current localhost:8080 build: title and comic intro are readable, but the 3D lab is too dark and first-person exploration hides spatial information.
- Design direction chosen: third-person cute probe robot, readable lab lighting, scanning-before-collection evidence loop, fixed-question persona interrogation, multiple endings.
- GitHub reference: ringhyacinth/hyacinth.im-site exposes a built asset tree with GLB models, textured environment props, grouped scene composition, readable colors, subtle animated environmental layers, and object-centric navigation. Use this as an architectural/staging reference, not as copied code.
- Implemented in `index.html`: open-front diorama 3D lab, cute M-07 "Doubao" probe robot, Space/on-screen scan, evidence collection popup, ZERO as third suspect, local persona interrogation engine, and five branching endings.
- Removed the exposed DeepSeek API key and replaced open-ended LLM calls with fixed-question character replies that change when evidence is shown.
- Verification: `node scripts/test_game_static.mjs` passes. Browser check at http://localhost:8080 confirms title, briefing, robot scene, scan hint, scan button, first evidence popup, and no console errors.
- Note: `$HOME/.codex/skills/develop-web-game/scripts/web_game_playwright_client.js` was attempted but could not run because package `playwright` is not available to that script in this environment.
- Generated and integrated a full asset pass under `assets/generated/`: lab floor texture, damaged wall texture, explosion scorch decal, corrupted monitor texture, robot-entry story panel, suspect-interrogation story panel, and mirror-reveal story panel.
- Updated the 3D room to use generated image textures through `THREE.TextureLoader` instead of only procedural colors. The generated scorch decal is now placed over the blast center, while floor/wall/monitor textures are mapped onto the lab surfaces and screens.
- Localized Three.js runtime files under `vendor/three` and switched the import map away from CDN URLs so the local competition build does not stall on external module loading.
- Updated story flow and evidence popups to use the newly generated panels: M-07 entering the irradiated lab, the three suspects, and the final Yuki/mirror reversal.
- Verification: `node scripts/test_game_static.mjs` passes. HTTP checks confirm `vendor/three/build/three.module.js` and `assets/generated/lab-floor-texture.png` return 200 from `localhost:8080`. Browser check confirms the 3D canvas mounts, HUD shows the robot scene, scan/use buttons collect evidence, the generated mirror image appears in the clue popup, and no browser console errors were reported.
- New direction from user: unify the game as a bold adult sci-fi cartoon rather than mixing realistic images, reduce exploration operators to one space detective, and rename the robot to `空间者1号`.
- Generated and integrated a cartoon asset set under `assets/generated/`: `cartoon-detective-operator.png`, `cartoon-spacefarer-entry.png`, `cartoon-lab-explosion.png`, `cartoon-mirror-reveal.png`, `cartoon-lab-floor-texture.png`, `cartoon-lab-wall-texture.png`, and `cartoon-monitor-texture.png`.
- Updated `index.html`: opening comic is now 5 panels and no longer shows multiple investigator faces; briefing/HUD/hints/voice lines use `空间者1号`; suspect buttons no longer use old inconsistent portraits; evidence popups use cartoon generated images.
- Updated 3D lab: floor/wall/monitor now use cartoon texture assets; removed the obvious geometric floor grid overlay; added full-room green radiation puddles and crack meshes so the floor does not look like one isolated decal in a plain grid room; wall opacity increased and texture panels are larger.
- Verification: `node scripts/test_game_static.mjs` passes. Browser flow verified through start comic -> briefing -> 3D scene -> scan -> collect first evidence. The page shows `空间者1号`, loads the cartoon detective first panel, mounts one WebGL canvas, opens the cartoon mirror evidence popup, and has no stale `豆包`/`M-07`/`RIN` names in visible UI.
- User clarified that removing all character images made the cast unclear. Generated and integrated consistent cartoon portraits for Yuki, MORAN, LILA, and ZERO under `assets/generated/cartoon-*-portrait.png`.
- Added cast cards to the briefing screen and restored portrait avatars in interrogation buttons and suspect dialogue while keeping the single space detective as the only operator.
- Added voice profiles through the browser SpeechSynthesis API: narrator/detective/MORAN/LILA/ZERO use different rate and pitch profiles and try to pick available Chinese voices.
- Added a visible evidence board in interrogation: collected clues now appear as cards, can be clicked to show evidence, and are still listed in the sidebar for cross-examination.
- Added a local "AI assistant" style loop without external API keys: free-form questions are keyword-routed to persona replies, then the assistant summarizes which contradiction slot was hit and whether the target is likely relevant to the hidden truth.
- Added randomized hidden truth per new game (`moran`, `lila`, `zero`, or `self`). The correct ending is now determined by the current randomized truth, and the reveal screen reports the actual hidden truth after the player's choice.
- Added a two-phase structure: phase 1 public search -> first interrogation -> AI contradiction summary -> phase 2 second search with truth-dependent private clues -> second interrogation -> final deduction.
- Verification: static test and extracted module syntax check pass. Browser check confirms briefing portraits load, 3D scene mounts, no console errors appear on start/briefing/scene flow, and stale names remain absent from visible UI.
- 2026-05-31: Generated a new evidence/tribunal art pass with Codex image generation and copied project-bound assets into `assets/generated/`: `evidence-reactor-core.png`, `evidence-terminal-glitch.png`, `evidence-authorization-file.png`, `evidence-surveillance-gap.png`, `evidence-yuki-note.png`, `evidence-zero-access.png`, and `scene-roundtable-tribunal.png`.
- Integrated those assets into the clue data and interrogation evidence board. Evidence cards now render image thumbnails instead of being text-only cards, and the interrogation screen uses the generated roundtable tribunal background.
- Added an interrogation pressure system: each suspect now has a tension meter, presenting evidence applies pressure based on evidence relevance, and high pressure triggers a short breakdown/confession-style line. `render_game_to_text` now exposes pressure/broken state for tests.
- Improved the local AI persona prompt so suspects know the hidden truth branch, whether they are guilty or merely covering a violation, and their current pressure level. This should reduce out-of-character replies.
- Added `docs/mg2026-ai-voice-playbook.md` with the competition-friendly plan: pre-generated key voice lines, no exposed frontend API keys, AI-enhanced interrogation as optional, and a PDF report outline.
- 2026-05-31 continuation: Fixed opening comic pacing so panels hold based on narration length instead of auto-flipping after 3.8s; narrator voice profile is slower.
- Reworked interrogation from a plain chat panel into a visible roundtable stage with three suspect seats, active speech bubble, center evidence display, and evidence cards with text overlays on generated images.
- Added non-repeating hidden truth selection via `mirrorlab-last-truth` to reduce the feeling that MORAN is always guilty.
- Rebalanced pressure: irrelevant evidence no longer raises every suspect's tension; only truth-relevant or cover-up-relevant evidence/question challenges increase pressure.
- Removed early AI-summary leakiness. First-round summaries now describe contradiction areas without naming the hidden truth.
- Added `scripts/deepseek_proxy.mjs`; the game now prefers local DeepSeek proxy using `deepseek-v4-flash`, then falls back to Ollama, then to deterministic persona replies. Proxy can read `DEEPSEEK_API_KEY` or the local OpenClaw DeepSeek config without embedding a key in frontend code.
- 2026-06-01: Added roundtable interjections. After the player questions one suspect, another suspect can interrupt: if the hidden truth is an external suspect and the player is questioning someone else, the culprit tends to redirect suspicion; if the player is on the culprit, other suspects challenge or protect themselves. This makes the interrogation less one-line and more social-deduction-like.
- Updated DeepSeek handling after testing `deepseek-v4-flash`: the proxy now sends `thinking: { type: "disabled" }`, and the game can recover from `reasoning_content` if the provider returns it. Short/empty AI replies still fall back to persona lines.
- Tightened suspect knowledge boundaries: individual suspect prompts no longer receive the full hidden-truth rule as global knowledge. Instead, `roleKnowledge` gives each character only their own secret, suspicion, and motive; true culprits know their own cover story, while non-culprits only suspect others. This should reduce accidental AI泄题.
- Added `personaVariant`, a truth-aware deterministic fallback, so rule-mode replies also vary by hidden truth instead of always returning the same obvious canned answer.
- 2026-06-01 continuation: Replaced the old final "pick one culprit" ending screen with a three-part deduction tribunal. Players must now choose the responsibility core, the locked-room mechanism, and a key evidence card; the reveal scores each segment separately against the randomized hidden truth. This makes the final step a real evidence-chain check instead of a simple suspect vote.
- Updated `render_game_to_text` and static checks so the final deduction state (`finalPick`), mechanism options, truth-specific solutions, and scored reveal path remain covered.
- Verification: `node scripts/test_game_static.mjs`, extracted module `node --check`, and `node --check scripts/deepseek_proxy.mjs` pass. `curl` confirms both `localhost:8080` and the `deepseek-v4-flash` proxy health endpoint respond. A direct DeepSeek proxy request returned a role line. The web-game Playwright client still cannot run because the skill script cannot import package `playwright` in this environment.
- 2026-06-01 continuation: Tightened the opening comic pacing again. `spk()` now resolves against both narration completion/fallback and minimum hold time, comic panels hold longer, and the screen has a locked "朗读中…" / "下一幕" control so panels do not appear to jump before the story line is finished.
- Strengthened the roundtable presentation: the current speaking suspect seat is highlighted, the stage has a visible roundtable feed with recent discussion chips, and the feed displays the active AI path/latency (`DeepSeek Flash`, local model, or rules fallback). `render_game_to_text` now exposes `aiTrace` for verification.
- Verification: static checks and extracted module syntax checks pass again; `curl` confirms the DeepSeek V4 Flash proxy and `localhost:8080` are responding. The Playwright web-game client is still blocked by missing package `playwright` in the skill script environment, so visual verification remains manual via the opened browser tab.
- 2026-06-01 continuation: Evidence presentation now triggers an actual roundtable action instead of only raising pressure. `showE` is async: the evidence is pushed to the table, pressure is applied only when relevant, the target suspect responds through DeepSeek/local/rule fallback, another suspect may interject, and the action advances the interrogation round. Added `buildEvidencePrompt`, `evidenceReply`, and truth-aware fallback evidence reactions so irrelevant evidence can be deflected instead of making everyone panic.
- Verification: static checks and extracted module syntax checks pass. DeepSeek V4 Flash health is ready, `localhost:8080` responds, and a direct DeepSeek proxy call returned a fast role-style evidence reaction.
- 2026-06-01 continuation: Made the roundtable feel less like a transcript. Each suspect seat now has a speech bubble (`seat-talk`) that updates when that character speaks, the active speaker still highlights, the main stage is taller, and the bottom transcript has been visually deemphasized. `render_game_to_text` now exposes `seatLines` for verification.
- Verification: static checks and extracted module syntax checks pass; DeepSeek V4 Flash health and `localhost:8080` both respond.
- 2026-06-01 continuation: Reworked hidden-truth randomization from "avoid only the previous truth" into a persistent shuffled truth deck. `drawTruthBranch` now stores `mirrorlab-truth-deck` and draws MORAN/LILA/ZERO/self without repeats until all four have appeared, then reshuffles. The briefing shows a non-spoiler `CASE SEED` and deck remaining count so replay variation is visible without revealing the answer.
- Verification: static checks, extracted module syntax checks, `localhost:8080`, and DeepSeek V4 Flash health all pass. A local Node simulation drew four hidden truths and confirmed no early repeats.
- 2026-06-01 continuation: Added a sidebar contradiction board. Free questions, evidence presentations, and AI summaries now create compact logic cards (`contradictions`) tagged by time line, motive, surveillance gap, experiment authority, evidence hit/miss, etc. This lets the player accumulate usable deduction material without rereading the full transcript, while still avoiding direct culprit leaks.
- Verification: static checks, extracted module syntax checks, `localhost:8080`, and DeepSeek V4 Flash health all pass.
- 2026-06-01 continuation: Brought contradiction cards into the final tribunal. The deduction screen now starts with a "圆桌矛盾摘录" section that carries up to six logic cards from the interrogation before the player picks responsibility, mechanism, and proof. This connects AI-assisted discussion to the final evidence-chain puzzle without changing the scoring rules or leaking the culprit.
- Verification: static checks, extracted module syntax checks, `localhost:8080`, and DeepSeek V4 Flash health all pass.
- 2026-06-01 continuation: Added DeepSeek/local `tacticalReview` for per-action contradiction comparison. After free questions or evidence presentation, the AI assistant now compares the player action, evidence, suspect reply, recent roundtable notes, pressure state, and hidden branch rules, then produces a short non-spoiler tactical review card. These cards go into the contradiction board and can appear as AI assistant feedback, giving the player clearer next-step pursuit without revealing the culprit.
- Verification: static checks, extracted module syntax checks, `localhost:8080`, and DeepSeek V4 Flash health all pass. A direct DeepSeek proxy tactical-review prompt returned a concise next-question suggestion.
- 2026-06-01 continuation: Added interrogation strategy modes to make player choices change the roundtable dynamic. The sidebar now has 套话 / 施压 / 设陷. The selected mode is stored as `interrogateMode`, included in suspect/evidence AI prompts, exposed via `render_game_to_text`, and affects pressure gain (`probe` lower, `press` higher, `trap` neutral but prompts more detailed lying). This makes the same question/evidence produce different styles of suspect behavior.
- Verification: static checks, extracted module syntax checks, `localhost:8080`, and DeepSeek V4 Flash health all pass.
- 2026-06-01 continuation: Made tactical reviews non-blocking. Suspect replies and interjections still complete before the round advances, but `queueTacticalReview` now runs the AI contradiction comparison in the background and appends the result to the contradiction board when ready. Added `isThinking` to prevent duplicate evidence/question clicks while primary AI output is generating, and `tacticalPending` to expose background review state.
- Verification: static checks, extracted module syntax checks, `localhost:8080`, and DeepSeek V4 Flash health all pass.
- 2026-06-01 continuation: Reduced premature solve leakage from high-pressure breakdowns. `breakdownLine` no longer lets the actual culprit directly confess the complete method (e.g. full forced-overload or rescue-delay admission). Instead, pressure now produces partial admissions, evasions, and emotionally defensive lines; the player still needs the final responsibility/mechanism/proof chain to solve the case.
- Verification: static checks and extracted module syntax checks pass. A text scan confirms the old direct-confession phrases are gone from `index.html`; `localhost:8080` and DeepSeek V4 Flash health both respond.
- 2026-06-01 continuation: Added a non-spoiler suspicion heat board. The sidebar now shows per-suspect heat derived from pressure, contradiction-card count, and evidence shown to that suspect. It explicitly says heat is not the culprit, and `render_game_to_text` exposes `suspicionHeat`. This helps the player decide who to question next without replacing final deduction.
- Verification: static checks, extracted module syntax checks, `localhost:8080`, and DeepSeek V4 Flash health all pass.
